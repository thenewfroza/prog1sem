#include <stdio.h>
#define M_PI
#include <math.h>
int main()
{
	int a;
	float b;
	float b1;
	b = (sin(4 * a) / (1 + cos(4 * a))) * (cos(2 * a) / (1 + cos(2 * a));
	b1 = ctg(((3 / 2) * M_PI) - a);
	return 0;
}
